import React, { useState } from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faBars, faTimes,faArrowRight,faArrowCircleLeft, faSearch, faArrowLeft } from '@fortawesome/free-solid-svg-icons';
import { Link, useNavigate } from 'react-router-dom';
import partyPalaceImage from '../img/s/party.jpg';
import party2 from '../img/s/bg.jpg';
import wedd from '../img/s/lanWedd.avif'
import wedd1 from '../img/s/kuriweed.jpg'
import wedd2 from '../img/s/wedd.jpg'
import wedd3 from '../img/s/sWeed.jpg'
import grad from '../img/s/milliGradua.jpeg'
import meet from '../img/s/sMeet.jpg'




import party3 from '../img/s/partty.jpg'
import bg from '../img/s/bg2.avif'

const Events = () => {
  const [isMenuOpen, setMenuOpen] = useState(false);
  const [searchTerm, setSearchTerm] = useState('');

  const handleMenuToggle = () => {
    setMenuOpen(!isMenuOpen);}
  const bestPlaces = [
    {
      id: 1,
      name: 'Party Palace',
      location: '123 Main St, City',
      description: 'A spacious venue with great amenities for parties.',
      imageUrl: party3,
    },
    {
      id: 2,
      name: 'Funland Park',
      location: '456 Elm St, Town',
      description: 'An amusement park with thrilling rides and party packages.',
      imageUrl: party2,
    },
    {
      id: 3,
      name: 'Water World',
      location: '789 Ocean Ave, Beachside',
      description: 'A water park with exciting slides and wave pools.',
      imageUrl: partyPalaceImage,
    },
    // Add more places as needed
  ];
  
  const wedding = [
    {
      id: 1,
      name: 'Lake Langano',
      location: 'Ziway',
      description: 'A spacious venue with great amenities for parties.',
      imageUrl: wedd,
    },
    {
      id: 2,
      name: 'Funland Park',
      location: '456 Elm St, Town',
      description: 'An amusement park with thrilling rides and party packages.',
      imageUrl: wedd2,
    },
    {
      id: 3,
      name: 'Water World',
      location: '789 Ocean Ave, Beachside',
      description: 'A water park with exciting slides and wave pools.',
      imageUrl:wedd1,
    },
    {
      id: 4,
      name: 'Water World',
      location: '789 Ocean Ave, Beachside',
      description: 'A water park with exciting slides and wave pools.',
      imageUrl:wedd3,
    },
  ]
    const Grad = [
      {
        id: 1,
        name: 'Party Palace',
        location: '123 Main St, City',
        description: 'A spacious venue with great amenities for parties.',
        imageUrl: grad,
      },
      {
        id: 2,
        name: 'Funland Park',
        location: '456 Elm St, Town',
        description: 'An amusement park with thrilling rides and party packages.',
        imageUrl: party2,
      },
      {
        id: 3,
        name: 'Water World',
        location: '789 Ocean Ave, Beachside',
        description: 'A water park with exciting slides and wave pools.',
        imageUrl: partyPalaceImage,
      },
  ];

  const Meet = [
    {
      id: 1,
      name: 'Sky Light Hotel',
      location: 'Addis Ababa',
      description: 'A spacious venue with great amenities for parties.',
      imageUrl: meet,
    },
    {
      id: 2,
      name: 'Funland Park',
      location: '456 Elm St, Town',
      description: 'An amusement park with thrilling rides and party packages.',
      imageUrl: party2,
    },
    {
      id: 3,
      name: 'Water World',
      location: '789 Ocean Ave, Beachside',
      description: 'A water park with exciting slides and wave pools.',
      imageUrl: partyPalaceImage,
    },
];

  const [currentSlide, setCurrentSlide] = useState(0);

  const goToPreviousSlide = () => {
    setCurrentSlide((prevSlide) => (prevSlide === 0 ? bestPlaces.length - 1 : prevSlide - 2));
  };

  const goToNextSlide = () => {
    setCurrentSlide((prevSlide) => (prevSlide === bestPlaces.length - 1 ? 0 : prevSlide + 2));
  };

  const visiblePlaces = [
    bestPlaces[currentSlide],
    bestPlaces[(currentSlide + 1) % bestPlaces.length]
  ];

  const [currentWeddingSlide, setCurrentWeddingSlide] = useState(0);

  const goToPreviousWeddingSlide = () => {
    setCurrentWeddingSlide((prevSlide) => (prevSlide === 0 ? wedding.length - 1 : prevSlide - 2));
  };

  const goToNextWeddingSlide = () => {
    setCurrentWeddingSlide((prevSlide) => (prevSlide === wedding.length - 1 ? 0 : prevSlide + 2));
  };

  const visibleWeddingPlaces = [
    wedding[currentWeddingSlide],
    wedding[(currentWeddingSlide + 1) % wedding.length]
  ];
 const [currentGrad, setCurrentGrad] = useState(0);

  const goToPreviousGrad = () => {
    setCurrentGrad ((prevGrad ) => (prevGrad  === 0 ? Grad.length - 1 : prevGrad - 2));
  };

  const goToNextGrad= () => {
    setCurrentGrad ((prevGrad ) => (prevGrad  === Grad.length - 1 ? 0 : prevGrad  + 2));
  };
  const visibleGrad = [
    Grad [currentGrad ],
    Grad [(currentGrad  + 1) % Grad .length]
  ];

  const [currentMeet, setCurrentMeet] = useState(0);

  const goToPreviousMeet = () => {
    setCurrentMeet ((prevMeet ) => (prevMeet  === 0 ? Meet.length - 1: prevMeet - 2));
  };

  const goToNextMeet= () => {
    setCurrentMeet ((prevMeet ) => (prevMeet  === Meet.length - 1 ? 0 : prevMeet  + 2));
  };

  const visibleMeet = [
    Meet [currentMeet ],
    Meet [(currentMeet  + 1) % Meet .length]
  ];
  return (

    <div className="bg-gray-300 ">
      <header className="relative flex justify-between  items-center bg-purple-950 z-20">

      <div className="absolute top-0 left-0 w-full h-full bg-black  bg-opacity-75 flex flex-col justify-center items-center">
        <div className="flex items-center">
          <h1 className="text-3xl font-bold text-white  tracking-wider font-cursive absolute left-6 top-8">
            Events
          </h1>
        </div>

      
        <div className="flex items-center ml-auto space-x-4">
          <nav className="hidden sm:flex space-x-4 absolute top-10 right-48 ">
            <Link to="/" className="text-white   hover:bg-purple-500 hover:bg-opacity-10 px-2 rounded-full">
              Home
            </Link>
            <Link to="/about" className="text-white  text-sm  hover:bg-purple-500 hover:bg-opacity-10 px-2 rounded-full">
              About
            </Link>
            <Link to="/register" className="text-white  text-sm  hover:bg-purple-500 hover:bg-opacity-10 px-2 rounded-full">
              Services
            </Link>
            <Link to="/contact" className="text-white  text-sm  hover:bg-purple-500 hover:bg-opacity-10 px-2 rounded-full">
              Contact
            </Link>
          </nav>

          <div className="sm:hidden ">
            <button
              className="text-white hover:text-white focus:outline-none "
              onClick={handleMenuToggle}
            >
              <div className='absolute top-8 right-4'>
                {isMenuOpen ? (
                  <FontAwesomeIcon icon={faTimes} className="w-4 h-4" />
                ) : (
                  <FontAwesomeIcon icon={faBars} className="w-4 h-4" />
                )}
              </div>
            </button>
          </div>

          <div className="hidden sm:flex items-center space-x-4 absolute top-8 right-4">
            <button className="bg-white hover:bg-gray-300 hover:text-black border-gray-500 border-r-2 text-sm text-black px-4 py-2 rounded-l-full relative left-4">
              Sign In
            </button>
            <button className="bg-white hover:bg-gray-300 hover:text-black text-bold  text-black px-4 py-2  text-sm  rounded-r-full ">
              Log In
            </button>
          </div>
        </div>
      </div>

      {isMenuOpen && (
        <div className="sm:hidden bg-gray-400 absolute top-16 right-4 bg-opacity-10">
          <nav className="px-2 pt-2 pb-2 space-y-1">
            <Link to="/" className="block text-white hover:text-white">
              Home
            </Link>
            <Link to="/suggestion" className="block text-white hover:text-white">
              About
            </Link>
            <Link to="/events" className="block text-white hover:text-white">
              Services
            </Link>
            <Link to="/contact" className="block text-white hover:text-white">
              Contact
            </Link>
            <div className="flex items-center mt-4">
              <button className="bg-white hover:bg-gray-300 text-black px-2 py-2 rounded">
                Sign In
              </button>
              <button className="bg-white hover:bg-gray-300 text-black px-2 py-2 rounded ml-2">
                Sign Up
              </button>
            </div>
          </nav>
        </div>
      )}
    </header>
    <div className="relative bg-black bg-opacity-75">
  <img
    src={bg}
    alt="Background"
    className="w-full h-auto object-cover opacity-75"
    style={{ maxHeight: '600px' }}
  /> 
  <div className="absolute top-0 right-0 w-full h-full bg-black text-white bg-opacity-50 flex flex-col justify-center items-center" />
  <div className="absolute top-0 left-0 w-3/5 p-4 h-full bg-white mb-32 text-white bg-opacity-5 flex flex-col justify-center items-center">
    <p className="font-bold lg:text-3xl md:text-3xl sm:text-xl  leading-tight text-center">
      Discover the perfect places for your special moments.
    </p>
    <p className="text-lg  leading-tight text-center pt-6">
      Elevate your occasion with our curated selection.
    </p>
    <p className="text-lg leading-tight text-center p-1">
      Find your ideal setting.Create lasting memories.
    </p> 
    
  </div>
</div>

<div className="mx-auto bg-gray-300">
  <h1 className="text-xl text-black mb-2 text-center pt-4">Top Categories with Preferable Venues and Hotels Places</h1>
  <h1 className="text-lg text-black mb-2 text-center">For Special Moments</h1>

  <div className="grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 gap-4">
    {visiblePlaces.map((place, index) => (
      <div
        key={place.id}
        className="bg-white rounded-md overflow-hidden shadow-lg relative"
      >
        <div className="h-64 md:h-48 lg:h-64">
          <img className="h-full w-full object-cover" src={place.imageUrl} alt={place.name} />
          {index === 0 && (
            <button
              className="bg-gray-800 text-white py-2 px-4 rounded-l-md absolute top-1/2 left-4 transform -translate-y-1/2"
              onClick={goToPreviousSlide}
            >
              <FontAwesomeIcon icon={faArrowLeft} className="w-4 h-4" />
            </button>
          )}
          {index === 1 && (
            <button
              className="bg-gray-800 text-white py-2 px-4 rounded-r-md absolute top-1/2 right-4 transform -translate-y-1/2"
              onClick={goToNextSlide}
            >
              <FontAwesomeIcon icon={faArrowRight} className="w-4 h-4" />
            </button>
          )}
        </div>
        <div className="p-1 absolute bottom-0 left-0 w-1/3 h-16 bg-white bg-opacity-75">
          <h2 className="text-xl font-bold mb-1 text-black">{place.name}</h2>
          <p className="text-black mb-4">{place.location}</p>
        </div>
      </div>
    ))}
  </div>

      </div>
      <div className="mx-2 mt-8">
  <h1 className="text-xl font-bold text-black mb-6 text-left">Best Places for a Wedding</h1>
  <div className="grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 gap-4">
    {visibleWeddingPlaces.map((place, index) => (
      <div
        key={place.id}
        className="bg-white rounded-md overflow-hidden shadow-lg relative"
      >
        <div className="h-64 md:h-48 lg:h-64 relative">
          <img className="h-full w-full object-cover" src={place.imageUrl} alt={place.name} />
          <div className="absolute inset-0 flex items-center justify-between">
            {index === 0 && (
              <button
                className="bg-gray-800 text-white py-2 px-4 rounded-l-md"
                onClick={goToPreviousWeddingSlide}
              >
                <FontAwesomeIcon icon={faArrowLeft} className="w-4 h-4" />
              </button>
            )}
            {index === 1 && (
              <button
                className="bg-gray-800 text-white py-2 px-4 rounded-r-md"
                onClick={goToNextWeddingSlide}
              >
                <FontAwesomeIcon icon={faArrowRight} className="w-4 h-4" />
              </button>
            )}
          </div>
        </div>
        <div className="p-1 absolute bottom-0 left-0 w-1/3 h-16 bg-white bg-opacity-75">
          <h2 className="text-xl font-bold mb-1 text-black">{place.name}</h2>
          <p className="text-black mb-4">{place.location}</p>
        </div>
      </div>
    ))}
  </div>
      </div>

      <div className="mx-2 mt-8">
  <h1 className="text-xl font-bold text-black mb-4 text-left">Best Places for a Graduation</h1>
  <div className="grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 gap-4">
    {visibleGrad.map((place, index) => (
      <div
        key={place.id}
        className="bg-white rounded-md overflow-hidden shadow-lg relative"
      >
        <div className="h-64 md:h-48 lg:h-64 relative">
          <img className="h-full w-full object-cover" src={place.imageUrl} alt={place.name} />
          {index === 0 && (
            <div className="absolute inset-0 flex items-center justify-between">
              <button
                className="bg-gray-800 text-white py-2 px-4 rounded-l-md"
                onClick={goToPreviousGrad}
              >
                <FontAwesomeIcon icon={faArrowLeft} className="w-4 h-4" />
              </button>
            </div>
          )}
          {index === 1 && (
            <div className="absolute inset-0 flex items-center justify-between">
              <button
                className="bg-gray-800 text-white py-2 px-4 rounded-r-md"
                onClick={goToNextGrad}
              >
                <FontAwesomeIcon icon={faArrowRight} className="w-4 h-4" />
              </button>
            </div>
          )}
        </div>
        <div className="p-1 absolute bottom-0 left-0 w-1/3 bg-white h-16 bg-opacity-75">
          <h2 className="text-xl font-bold mb-1 text-black">{place.name}</h2>
          <p className="text-black mb-4">{place.location}</p>
        </div>
      </div>
    ))}
  </div>
</div>

        
  
<div className="mx-2 mt-8">
  <h1 className="text-xl font-bold text-black mb-4 text-left">Best Places for a Meeting</h1>
  <div className="grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 gap-4">
    {visibleMeet.map((place, index) => (
      <div
        key={place.id}
        className="bg-white rounded-md overflow-hidden shadow-lg relative"
      >
        <div className="h-64 md:h-48 lg:h-64">
          <img className="h-full w-full object-cover" src={place.imageUrl} alt={place.name} />
          {index === 0 && visibleMeet.length > 1 && (
            <button
              className="bg-gray-800 text-white py-2 px-4 rounded-l-md absolute top-1/2 left-4 transform -translate-y-1/2"
              onClick={goToPreviousMeet}
            >
              <FontAwesomeIcon icon={faArrowLeft} className="w-4 h-4" />
            </button>
          )}
          {index === 1 && visibleMeet.length > 1 && (
            <button
              className="bg-gray-800 text-white py-2 px-4 rounded-r-md absolute top-1/2 right-4 transform -translate-y-1/2"
              onClick={goToNextMeet}
            >
              <FontAwesomeIcon icon={faArrowRight} className="w-4 h-4" />
            </button>
          )}
        </div>
        <div className="p-1 absolute bottom-0 left-0 w-1/3 bg-white h-16 bg-opacity-75">
          <h2 className="text-xl font-bold mb-1 text-black">{place.name}</h2>
          <p className="text-black mb-4">{place.location}</p>
        </div>
      </div>
    ))}
  </div>
 
</div>
    </div>
  );
};

export default Events;
